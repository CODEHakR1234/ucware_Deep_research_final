# ───────── FastAPI 기본 서버 ─────────
fastapi
uvicorn
requests
python-dotenv

# ───────── PDF 및 문서 처리 ─────────
PyMuPDF>=1.23.0  # ColPali용 PDF → 이미지 변환
Pillow>=10.0.0   # ColPali용 이미지 처리  

# ───────── LangChain 최신 구조 (0.2+) ─────────
langchain==0.2.16
langchain-openai==0.1.21
langchain-chroma>=0.1.4  # ChromaDB 최신 버전 호환성
langchain-community==0.2.16

# ───────── LangGraph 요약 에이전트 구성 ─────────
langgraph==0.2.3
# (주의) langchain-core는 적지 마세요

# LangChain experimental splitter
langchain-experimental>=0.0.15

# ───────── 벡터 임베딩 및 벡터 DB ─────────
sentence-transformers
chromadb[server]>=0.5.20  # 최신 안정 버전 (0.5.x 계열)

# ───────── 머신러닝 및 유사도 계산 ─────────
scipy>=1.11
numpy>=1.24.0

# ───────── 기타 ─────────
pydantic
tqdm
openai
redis>=5.0.0
httpx[http2]  # httpx 비동기 클라이언트
Jinja2>=3.0

# ───────── SmolDocling & Docling ─────────
docling>=0.6.0
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
accelerate>=0.20.0  # GPU 가속용

# ───────── Tavily Search API ─────────
tavily-python